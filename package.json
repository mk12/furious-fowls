{
  "name": "furious-fowls",
  "scripts": {
    "dev": "esbuild src/app.ts --servedir=. --outdir=dist --loader:.png=dataurl --bundle --sourcemap",
    "build": "esbuild src/app.ts --outdir=dist --loader:.png=dataurl --bundle --minify",
    "tc": "tsc --noEmit",
    "lint": "eslint --fix src/**/*.ts",
    "fmt": "prettier -w src/**/*.ts"
  },
  "devDependencies": {
    "@types/p5": "^0.9.0",
    "@typescript-eslint/eslint-plugin": "^4.31.0",
    "@typescript-eslint/parser": "^4.31.0",
    "esbuild": "^0.12.25",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "prettier": "^2.3.2",
    "typescript": "^4.4.2"
  },
  "prettier": {
    "trailingComma": "all"
  }
}
